services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    volumes:
      - ./flask_compendium:/app
    env_file:
      - .env
    command: flask run --host=0.0.0.0 --reload
